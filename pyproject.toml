[project]
name = "python-scripting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.62.0",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
